<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://davdma.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://davdma.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-08-28T03:08:51+00:00</updated><id>https://davdma.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Welford’s Online Algorithm</title><link href="https://davdma.github.io/blog/2025/Welfords-Algo/" rel="alternate" type="text/html" title="Welford’s Online Algorithm"/><published>2025-08-26T15:12:00+00:00</published><updated>2025-08-26T15:12:00+00:00</updated><id>https://davdma.github.io/blog/2025/Welfords-Algo</id><content type="html" xml:base="https://davdma.github.io/blog/2025/Welfords-Algo/"><![CDATA[<h1 id="motivation">Motivation</h1> <p>Up to this point, I have always lazily coded the mean std computation using the standard definition. The reason being that I like to stick to simple solutions until I encounter a case where it no longer works (my personal variant of Keep It Simple Stupid). But I had a rude awakening when I realized that being a simpleton was not going to cut it anymore for my flood mapping ML project. I needed to normalize my dataset for my ML experiments, but reading the enormous amounts of satellite data into memory and calculating the mean and std across it in two passes was a horrific approach to say the least. Luckily I had heard about the Welford’s Online Algorithm.</p> <h1 id="math-background">Math Background</h1> <p>To understand Welford’s Algorithm, we first start with a derived identity for single pass variance computation from the standard definition:</p> \[\sigma^2 = \frac{\sum_{i=1}^{N} (x_i - \bar{x})^2}{N - 1}\] <p>where \(\bar{x} = \frac{1}{N} \sum_{i=1}^{N} x_i\).</p> <p>Expanding the square, we get</p> \[\begin{align} \sigma^2 &amp;= \frac{\sum_{i=1}^{N} (x_i^2 - 2x_i \bar{x} + \bar{x}^2)}{N - 1} \\ &amp;= \frac{(\sum_{i=1}^{N} x_i^2) - 2 (\sum_{i=1}^{N} x_i) (\frac{1}{N} \sum_{i=1}^{N} x_i) + N\bar{x}^2}{N - 1} \\ &amp;= \frac{(\sum_{i=1}^{N} x_i^2) - 2N \bar{x}^2 + N\bar{x}^2}{N - 1} \\ &amp;= \frac{(\sum_{i=1}^{N} x_i^2) - N\bar{x}^2}{N - 1} \end{align}\] <p>Now we can calculate the mean and variance in one pass by keeping track of the sums \(\sum_{i=1}^{N} x_i^2\) and \(\sum_{i=1}^{N} x_i\).</p> <p>However, this approach for calculating variance is <strong>not numerically stable</strong> due to the nature of floating point precision. In the case that variance is small and the data is tightly clustered together, \(\sum x_i^2\) and the square of the mean \(N \bar{x}^2\) can differ very little from each other (usually they happen to be two large numbers with a difference orders of magnitude smaller). Subtracting the two results in catastrophic cancellation due to the loss of the significant digits.</p> <h1 id="interlude-ieee-floating-points">Interlude: IEEE floating points</h1> <p>Before continuing, I want to elaborate a bit on floating point precision and catastrophic cancellation. This requires a bit of knowledge about floating points. A 32 bit floating point number has 1 sign bit, 8 exponent bits, and 23 mantissa bits. There are also two important subdivisions of floating point values based on the exponent bits: denormalized and normalized. Subnormals or denormalized floating points are those with exponent bits all set to zero and approximate dense values close to zero on the real number line. On the other hand normalized floating points are those with exponent bits that are neither all nonzero or all ones and approximate values further out from zero.</p> <p>When calculating the value of a <strong>normalized</strong> floating point representation, the exponent has a bias of <code class="language-plaintext highlighter-rouge">127</code> that is subtracted from the binary value in order to get the final value in the range of <code class="language-plaintext highlighter-rouge">[-126, 127]</code>. The ultimate value is:</p> \[(-1)^{s} \times 2^{(e_7e_6e_5e_4e_3e_2e_1e_0)_2 - 127} \times (1 + \sum_{i=1}^{23} b_{23-i} \times 2^{-i})\] <p>where \(s\) is the sign bit, \((e_7e_6e_5e_4e_3e_2e_1e_0)_2\) represents the 8 exponent bits in binary, and $b_i$ are the 23 mantissa bits. Here the exponent bits specify which powers of two you are between, so the interval is \([2^{e-127}, 2^{e-126})\) where \(e\) is the value of the 8 exponent bits.</p> <p>When calculating the value of <strong>denormalized</strong> floating point representation, the exponent is treated as if it were 1 (before subtracting the bias of 127), and there is no implicit leading 1 in the mantissa. The ultimate value is:</p> \[(-1)^{s} \times 2^{-126} \times (\sum_{i=1}^{23} b_{23-i} \times 2^{-i})\] <p>where the exponent is fixed at -126 and the mantissa directly represents the fractional part without the implicit leading 1.</p> <p>There are also more nuances to floating points when the exponent bits are <strong>all 1</strong>. If exponent bits are all one it represents inf or nan with the mantissa becoming the payload. You can read more about it in Goldberg’s paper <a href="https://dl.acm.org/doi/10.1145/103162.103163">What Every Computer Scientist Should Know About Floating Point Arithmetic</a>.</p> <p>So… how does floating point numbers cause catastrophic cancellation? As seen above, the precision of the floating point number is limited to what is representable by the exponent and mantissa bits. Between the interval <code class="language-plaintext highlighter-rouge">[2^2, 2^3)</code> with 23 mantissa bits, we have \(\frac{2^2}{2^{23}} \approx 4.76 \times 10^{-7}\) as the spacing between consecutive representable values (this exact difference to get to the next nearest representable floating point value is called the unit in last place or <code class="language-plaintext highlighter-rouge">ulp</code>). This relative precision of roughly <strong>7 digits</strong> is constant for float 32, and it is <strong>16 digits</strong> for float 64 (from 52 mantissa bits). Catastrophic cancellation occurs when we subtract two nearly equal large numbers which causes the significant digits to cancel out, leaving only the trailing digits which contain rounding errors to remain. The relative error of the resulting difference from the true difference can thus be large. To illustrate, if both numbers are around \(10^4\) but differ by only \(10^{-2}\), the subtraction will eliminate 6 significant digits, leaving us with a result that may have little to no accuracy due to the limited precision of floating point representation.</p> <h1 id="welfords-algorithm">Welford’s Algorithm</h1> <p>Now back to Welford’s. What is the workaround here?</p> <p>Welford’s looks at the difference between the sums of squared differences to the mean for \(N\) and \(N-1\) samples:</p> \[\begin{align} \sum_{i=1}^{N} (x_i - \bar{x}_N)^2 - \sum_{i=1}^{N-1} (x_i - \bar{x}_{N-1})^2 &amp;= (x_N - \bar{x}_N)^2 + \sum_{i=1}^{N-1} (x_i - \bar{x}_N)^2 - \sum_{i=1}^{N-1} (x_i - \bar{x}_{N-1})^2 \\ &amp;= (x_N - \bar{x}_N)^2 + \sum_{i=1}^{N-1} [(x_i - \bar{x}_N)^2 - (x_i - \bar{x}_{N-1})^2] \\ &amp;= (x_N - \bar{x}_N)^2 + \sum_{i=1}^{N-1} (x_i - \bar{x}_N + x_i - \bar{x}_{N-1})(\bar{x}_{N-1} - \bar{x}_N) \\ &amp;= (x_N - \bar{x}_N)^2 + \sum_{i=1}^{N-1} (2x_i - \bar{x}_N - \bar{x}_{N-1})(\bar{x}_{N-1} - \bar{x}_N) \\ &amp;= (x_N - \bar{x}_N)^2 + (\bar{x}_{N-1} - \bar{x}_N) \sum_{i=1}^{N-1} (2x_i - \bar{x}_N - \bar{x}_{N-1}) \end{align}\] <p>We have that the term,</p> \[\sum_{i=1}^{N-1} (2x_i - \bar{x}_N - \bar{x}_{N-1}) = 2(N-1)\bar{x}_{N-1} - (N-1)(\bar{x}_N + \bar{x}_{N-1}) = (N-1)(\bar{x}_{N-1} - \bar{x}_N)\] <p>The right term in Equation 9 becomes,</p> \[\begin{equation} (\bar{x}_{N-1} - \bar{x}_N) \sum_{i=1}^{N-1} (2x_i - \bar{x}_N - \bar{x}_{N-1}) = (\bar{x}_{N-1} - \bar{x}_N)(N-1)(\bar{x}_{N-1} - \bar{x}_N) = (N-1)(\bar{x}_{N-1} - \bar{x}_N)^2 \end{equation}\] <p>Using the recursive mean update formula that \(\bar{x}_N = \frac{(N-1)\bar{x}_{N-1} + x_N}{N}\), we can rearrange it and get the fact that:</p> \[\begin{equation} \bar{x}_{N-1} - \bar{x}_N = \frac{\bar{x}_{N-1} - \bar{x}_N}{N} \end{equation}\] <p>and</p> \[\begin{equation} \bar{x}_N - x_N = \frac{(N-1)\bar{x}_{N-1}+x_N}{N} - x_N = \frac{N-1}{N}(\bar{x}_{N-1} - x_N) \end{equation}\] <p>Now using equation 10-12, the term becomes:</p> \[\begin{equation} (\bar{x}_{N-1} - \bar{x}_N)(N-1)(\bar{x}_{N-1} - \bar{x}_N) = (\bar{x}_{N-1} - \bar{x}_N) \frac{(N-1)}{N}(\bar{x}_{N-1} - x_N) = (\bar{x}_{N-1} - \bar{x}_N)(\bar{x}_N - x_N) \end{equation}\] <p>Finally,</p> \[\begin{align} \sum_{i=1}^{N} (x_i - \bar{x}_N)^2 - \sum_{i=1}^{N-1} (x_i - \bar{x}_{N-1})^2 &amp;= (x_N - \bar{x}_N)^2 + (\bar{x}_{N-1} - \bar{x}_N) \sum_{i=1}^{N-1} (2x_i - \bar{x}_N - \bar{x}_{N-1}) \\ &amp;= (x_N - \bar{x}_N)^2 + (\bar{x}_{N-1} - \bar{x}_N)(\bar{x}_N - x_N) \\ &amp;= (x_N - \bar{x}_N)(x_N - \bar{x}_N - \bar{x}_{N-1} + \bar{x}_N) \\ &amp;= (x_N - \bar{x}_N)(x_N - \bar{x}_{N-1}) \end{align}\] <p>To calculate variance in a single pass, for each new data point say \(x_k\), you just need to iteratively update the mean \(\bar{x}_k = \bar{x}_{k-1} + \frac{x_k - \bar{x}_{k-1}}{k}\) and keep track of the difference of \(x_k\) to the current and previous mean. The product \((x_k - \bar{x}_k)(x_k - \bar{x}_{k-1})\) can then be added to a growing sum of squared differences term. At the end, simply divide the sum of squared differences by number of data points seen and you get the variance. From the iterative updates you will also have the full mean at the end.</p> <pre><code class="language-pseudocode">\begin{algorithm}
\caption{Welford's Single Pass Algorithm}
\begin{algorithmic}
\PROCEDURE{WelfordSinglePass}{$$\text{data}$$}
    \STATE $$n \leftarrow 0$$
    \STATE $$\text{mean} \leftarrow 0$$
    \STATE $$\text{sum\_of\_squares} \leftarrow 0$$
    \FOR{each $$x$$ in $$\text{data}$$}
        \STATE $$n \leftarrow n + 1$$
        \STATE $$\text{prev\_mean} \leftarrow \text{mean}$$
        \STATE $$\text{mean} \leftarrow \text{mean} + \frac{x - \text{mean}}{n}$$
        \STATE $$\text{sum\_of\_squares} \leftarrow \text{sum\_of\_squares} + (x - \text{mean}) \times (x - \text{prev\_mean})$$
    \ENDFOR
    \STATE $$\text{variance} \leftarrow \frac{\text{sum\_of\_squares}}{n - 1}$$
    \STATE \RETURN $$\text{mean}, \text{variance}$$
\ENDPROCEDURE
\end{algorithmic}
\end{algorithm}
</code></pre> <h1 id="parallelized-welford">Parallelized Welford</h1> <p>If a parallel processor is available, it is <a href="https://engineering.yale.edu/application/files/3817/3714/7395/tr222.pdf">recommended</a> to split the data into smaller samples and then compute the sum of squares for each sample individually. Then the global sum of squares can be computed by combining these smaller sums.</p>]]></content><author><name></name></author><category term="tech"/><category term="math"/><category term="code"/><category term="learning"/><summary type="html"><![CDATA[Learning the math behind single-pass mean std calculation]]></summary></entry><entry><title type="html">The Dining Philosophers</title><link href="https://davdma.github.io/blog/2025/Dining-Philosophers/" rel="alternate" type="text/html" title="The Dining Philosophers"/><published>2025-08-18T21:00:00+00:00</published><updated>2025-08-18T21:00:00+00:00</updated><id>https://davdma.github.io/blog/2025/Dining-Philosophers</id><content type="html" xml:base="https://davdma.github.io/blog/2025/Dining-Philosophers/"><![CDATA[<h1 id="the-problem">The Problem</h1> <p>This is a classical computer science concurrency problem with limited resources. The problem statement is as follows: imagine 5 philosophers dining at a round table, each with their own plate of spaghetti in front of them. There are only 5 forks, one between each philosopher. A philosopher can be in one of two states: thinking or eating. They are only allowed to eat when they have both the left and right fork (the limited resource). A key constraint is that they can only pick up one fork at a time, never both simultaneously. They also cannot attempt to pick up a fork that is already in use by their neighbor. Once they are done eating, both forks are released. Assuming the philosophers cannot communicate when they start or finish eating, how do you design a procedure for the philosophers to eat without any one of them starving?</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/diningphil-480.webp 480w,/assets/img/diningphil-800.webp 800w,/assets/img/diningphil-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/diningphil.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> A Netflix show called Pantheon I was watching featured the dining philosophers problem, with chopsticks and tofu stir fry instead of the usual forks and spaghetti. </div> <p>In Operating Systems speak, the philosophers are processes, and the forks are limited resources that must be shared in synchronized fashion!</p> <p>An issue you want to avoid is ending up in deadlock, where you are stuck in a circular dependency. For example if the procedure requires the philosopher to first try to pick up the fork on their left and then wait for the right fork to be available, it is possible that each philosopher takes the fork to their left, and sits there waiting for the fork on their right to be set down. However, since the fork on the right is being held by a philosopher who is also waiting for the next fork, the situation cannot progress and all the philosophers will starve.</p> <h1 id="parity-solution">Parity Solution</h1> <p>One easy solution to avoid deadlock is to use parity of the philosophers to ensure that this circular waiting does not occur. Numbering philosophers one through five, odd philosophers first try to pick up the fork on their left, while even philosophers first try to pick up the fork on their right. This way when a philosopher acquires a fork, the other fork it waits on can only be the “second” fork for the neighbor and never the first fork, in which case it is guaranteed to be set down and available at some future point in time. For instance, if the even philosopher picks up the right fork and waits on the left fork there are two possibilities: either that left fork is available and he succeeds, or it is held by an odd philosopher who has acquired both forks and is currently eating, and will soon enough put the fork down. This solution is obviously asymmetric as there will be philosophers 1 and 5 sat next to each other, and is more effective when you have an even number of philosophers and forks.</p> <h1 id="hierarchical-solution">Hierarchical Solution</h1> <p>Another solution is to give an order to the forks or resources. Numbering the forks 1 through 5, set a rule in which each philosopher must first acquire the lower numbered fork before attempting to acquire the higher numbered one. In the case that the rank of the forks increases clockwise, 4 of the philosophers will have the lower numbered fork to their right and will be “right-handed” while one philosopher with the 1 fork to the left and the 5 fork to their right will be “left-handed”. This circumvents the deadlock by preventing circular waiting. If each philosopher simultaneously obtains their lower numbered fork, only four can succeed, with one of the four philosophers (i.e. with 5 fork on left and 4 fork on right) free to acquire the highest numbered fork as their second and proceed.</p> <h1 id="waiter-solution">Waiter Solution</h1> <p>A more straightforward and obvious solution is to put the action of checking/changing fork states (free or in-use) inside a critical section. The analogy is that the philosopher must obtain permission to eat from a “waiter”. When requesting permission, the waiter first checks the availability of the two forks if they are free. If so, the waiter marks them as in-use, and gives permission to the philosopher to eat. In practice, the waiter is represented as a mutex lock, which once acquired, checks fork states. The mutex is released if one of the two forks is not free. If both forks are free, their states are changed to in-use before the lock is released (in which case the philosopher has permission to pick them up). This way, we make checking and picking up two forks atomic rather than sequential. This prevents any philosopher from being caught waiting for the second fork while holding the first, or for a neighbor to take a fork before the philosopher tries to pick it up. This can have good parallelism as failing to acquire both forks (permission denied) does not prevent other philosophers from subsequently acquiring forks.</p> <h1 id="djikstras-solution">Djikstra’s Solution</h1> <p>The solution I found really interesting was the one proposed by Djikstra. It uses both mutexes and semaphores, and abstracts away individual forks. Instead of looking at the problem from the perspective of forks, Djikstra looks from the perspective of whether the neighboring philosophers are eating (if neighbors are not eating, then you can eat). The following is a C++ implementation which I have yanked from Wikipedia:</p> <div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;chrono&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;mutex&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;random&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;semaphore&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;thread&gt;</span><span class="cp">
</span>
<span class="k">constexpr</span> <span class="k">const</span> <span class="kt">size_t</span> <span class="n">N</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>  <span class="c1">// number of philosophers (and forks)</span>
<span class="k">enum</span> <span class="k">class</span> <span class="nc">State</span>
<span class="p">{</span>
    <span class="n">THINKING</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span>  <span class="c1">// philosopher is THINKING</span>
    <span class="n">HUNGRY</span> <span class="o">=</span> <span class="mi">1</span><span class="p">,</span>    <span class="c1">// philosopher is trying to get forks</span>
    <span class="n">EATING</span> <span class="o">=</span> <span class="mi">2</span><span class="p">,</span>    <span class="c1">// philosopher is EATING</span>
<span class="p">};</span>

<span class="kt">size_t</span> <span class="kr">inline</span> <span class="nf">left</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">i</span><span class="p">)</span>
<span class="p">{</span>
    <span class="c1">// number of the left neighbor of philosopher i, for whom both forks are available</span>
    <span class="k">return</span> <span class="p">(</span><span class="n">i</span> <span class="o">-</span> <span class="mi">1</span> <span class="o">+</span> <span class="n">N</span><span class="p">)</span> <span class="o">%</span> <span class="n">N</span><span class="p">;</span> <span class="c1">// N is added for the case when  i - 1 is negative</span>
<span class="p">}</span>

<span class="kt">size_t</span> <span class="kr">inline</span> <span class="nf">right</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">i</span><span class="p">)</span>
<span class="p">{</span>
    <span class="c1">// number of the right neighbor of the philosopher i, for whom both forks are available</span>
    <span class="k">return</span> <span class="p">(</span><span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">%</span> <span class="n">N</span><span class="p">;</span>
<span class="p">}</span>

<span class="n">State</span> <span class="n">state</span><span class="p">[</span><span class="n">N</span><span class="p">];</span>  <span class="c1">// array to keep track of everyone's both_forks_available state</span>

<span class="n">std</span><span class="o">::</span><span class="n">mutex</span> <span class="n">critical_region_mtx</span><span class="p">;</span>  <span class="c1">// mutual exclusion for critical regions for</span>
<span class="c1">// (picking up and putting down the forks)</span>
<span class="n">std</span><span class="o">::</span><span class="n">mutex</span> <span class="n">output_mtx</span><span class="p">;</span>  <span class="c1">// for synchronized cout (printing THINKING/HUNGRY/EATING status)</span>

<span class="c1">// array of binary semaphores, one semaphore per philosopher.</span>
<span class="c1">// Acquired semaphore means philosopher i has acquired (blocked) two forks</span>
<span class="n">std</span><span class="o">::</span><span class="n">binary_semaphore</span> <span class="n">both_forks_available</span><span class="p">[</span><span class="n">N</span><span class="p">]</span>
<span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">binary_semaphore</span><span class="p">{</span><span class="mi">0</span><span class="p">},</span> <span class="n">std</span><span class="o">::</span><span class="n">binary_semaphore</span><span class="p">{</span><span class="mi">0</span><span class="p">},</span>
    <span class="n">std</span><span class="o">::</span><span class="n">binary_semaphore</span><span class="p">{</span><span class="mi">0</span><span class="p">},</span> <span class="n">std</span><span class="o">::</span><span class="n">binary_semaphore</span><span class="p">{</span><span class="mi">0</span><span class="p">},</span>
    <span class="n">std</span><span class="o">::</span><span class="n">binary_semaphore</span><span class="p">{</span><span class="mi">0</span><span class="p">}</span>
<span class="p">};</span>

<span class="kt">size_t</span> <span class="nf">my_rand</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">min</span><span class="p">,</span> <span class="kt">size_t</span> <span class="n">max</span><span class="p">)</span>
<span class="p">{</span>
    <span class="k">static</span> <span class="n">std</span><span class="o">::</span><span class="n">mt19937</span> <span class="n">rnd</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">time</span><span class="p">(</span><span class="nb">nullptr</span><span class="p">));</span>
    <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">uniform_int_distribution</span><span class="o">&lt;&gt;</span><span class="p">(</span><span class="n">min</span><span class="p">,</span> <span class="n">max</span><span class="p">)(</span><span class="n">rnd</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">test</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">i</span><span class="p">)</span>
<span class="c1">// if philosopher i is hungry and both neighbors are not eating then eat</span>
<span class="p">{</span>
    <span class="c1">// i: philosopher number, from 0 to N-1</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">state</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">==</span> <span class="n">State</span><span class="o">::</span><span class="n">HUNGRY</span> <span class="o">&amp;&amp;</span>
        <span class="n">state</span><span class="p">[</span><span class="n">left</span><span class="p">(</span><span class="n">i</span><span class="p">)]</span> <span class="o">!=</span> <span class="n">State</span><span class="o">::</span><span class="n">EATING</span> <span class="o">&amp;&amp;</span>
        <span class="n">state</span><span class="p">[</span><span class="n">right</span><span class="p">(</span><span class="n">i</span><span class="p">)]</span> <span class="o">!=</span> <span class="n">State</span><span class="o">::</span><span class="n">EATING</span><span class="p">)</span>
    <span class="p">{</span>
        <span class="n">state</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">State</span><span class="o">::</span><span class="n">EATING</span><span class="p">;</span>
        <span class="n">both_forks_available</span><span class="p">[</span><span class="n">i</span><span class="p">].</span><span class="n">release</span><span class="p">();</span> <span class="c1">// forks are no longer needed for this eat session</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">think</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">i</span><span class="p">)</span>
<span class="p">{</span>
    <span class="kt">size_t</span> <span class="n">duration</span> <span class="o">=</span> <span class="n">my_rand</span><span class="p">(</span><span class="mi">400</span><span class="p">,</span> <span class="mi">800</span><span class="p">);</span>
    <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">lock_guard</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">mutex</span><span class="o">&gt;</span> <span class="n">lk</span><span class="p">(</span><span class="n">output_mtx</span><span class="p">);</span> <span class="c1">// critical section for uninterrupted print</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">i</span> <span class="o">&lt;&lt;</span> <span class="s">" is thinking "</span> <span class="o">&lt;&lt;</span> <span class="n">duration</span> <span class="o">&lt;&lt;</span> <span class="s">"ms</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">std</span><span class="o">::</span><span class="n">this_thread</span><span class="o">::</span><span class="n">sleep_for</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">chrono</span><span class="o">::</span><span class="n">milliseconds</span><span class="p">(</span><span class="n">duration</span><span class="p">));</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">take_forks</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">i</span><span class="p">)</span>
<span class="p">{</span>
    <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">lock_guard</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">mutex</span><span class="o">&gt;</span> <span class="n">lk</span><span class="p">{</span><span class="n">critical_region_mtx</span><span class="p">};</span>  <span class="c1">// enter critical region</span>
        <span class="n">state</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">State</span><span class="o">::</span><span class="n">HUNGRY</span><span class="p">;</span>  <span class="c1">// record fact that philosopher i is State::HUNGRY</span>
        <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">lock_guard</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">mutex</span><span class="o">&gt;</span> <span class="n">lk</span><span class="p">(</span><span class="n">output_mtx</span><span class="p">);</span> <span class="c1">// critical section for uninterrupted print</span>
            <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"</span><span class="se">\t\t</span><span class="s">"</span> <span class="o">&lt;&lt;</span> <span class="n">i</span> <span class="o">&lt;&lt;</span> <span class="s">" is State::HUNGRY</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
        <span class="p">}</span>
        <span class="n">test</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>                        <span class="c1">// try to acquire (a permit for) 2 forks</span>
    <span class="p">}</span>                                   <span class="c1">// exit critical region</span>
    <span class="n">both_forks_available</span><span class="p">[</span><span class="n">i</span><span class="p">].</span><span class="n">acquire</span><span class="p">();</span>  <span class="c1">// block if forks were not acquired</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">eat</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">i</span><span class="p">)</span>
<span class="p">{</span>
    <span class="kt">size_t</span> <span class="n">duration</span> <span class="o">=</span> <span class="n">my_rand</span><span class="p">(</span><span class="mi">400</span><span class="p">,</span> <span class="mi">800</span><span class="p">);</span>
    <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">lock_guard</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">mutex</span><span class="o">&gt;</span> <span class="n">lk</span><span class="p">(</span><span class="n">output_mtx</span><span class="p">);</span> <span class="c1">// critical section for uninterrupted print</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"</span><span class="se">\t\t\t\t</span><span class="s">"</span> <span class="o">&lt;&lt;</span> <span class="n">i</span> <span class="o">&lt;&lt;</span> <span class="s">" is eating "</span> <span class="o">&lt;&lt;</span> <span class="n">duration</span> <span class="o">&lt;&lt;</span> <span class="s">"ms</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">std</span><span class="o">::</span><span class="n">this_thread</span><span class="o">::</span><span class="n">sleep_for</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">chrono</span><span class="o">::</span><span class="n">milliseconds</span><span class="p">(</span><span class="n">duration</span><span class="p">));</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">put_forks</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">i</span><span class="p">)</span>
<span class="p">{</span>

    <span class="n">std</span><span class="o">::</span><span class="n">lock_guard</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">mutex</span><span class="o">&gt;</span> <span class="n">lk</span><span class="p">{</span><span class="n">critical_region_mtx</span><span class="p">};</span>    <span class="c1">// enter critical region</span>
    <span class="n">state</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">State</span><span class="o">::</span><span class="n">THINKING</span><span class="p">;</span>  <span class="c1">// philosopher has finished State::EATING</span>
    <span class="n">test</span><span class="p">(</span><span class="n">left</span><span class="p">(</span><span class="n">i</span><span class="p">));</span>               <span class="c1">// see if left neighbor can now eat</span>
    <span class="n">test</span><span class="p">(</span><span class="n">right</span><span class="p">(</span><span class="n">i</span><span class="p">));</span>              <span class="c1">// see if right neighbor can now eat</span>
                                 <span class="c1">// exit critical region by exiting the function</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">philosopher</span><span class="p">(</span><span class="kt">size_t</span> <span class="n">i</span><span class="p">)</span>
<span class="p">{</span>
    <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span>
    <span class="p">{</span>                         <span class="c1">// repeat forever</span>
        <span class="n">think</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>             <span class="c1">// philosopher is State::THINKING</span>
        <span class="n">take_forks</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>        <span class="c1">// acquire two forks or block</span>
        <span class="n">eat</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>               <span class="c1">// yum-yum, spaghetti</span>
        <span class="n">put_forks</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>         <span class="c1">// put both forks back on table and check if neighbors can eat</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"dp_14</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>

    <span class="n">std</span><span class="o">::</span><span class="n">jthread</span> <span class="n">t0</span><span class="p">([</span><span class="o">&amp;</span><span class="p">]</span> <span class="p">{</span> <span class="n">philosopher</span><span class="p">(</span><span class="mi">0</span><span class="p">);</span> <span class="p">});</span> <span class="c1">// [&amp;] means every variable outside the ensuing lambda</span>
    <span class="n">std</span><span class="o">::</span><span class="n">jthread</span> <span class="nf">t1</span><span class="p">([</span><span class="o">&amp;</span><span class="p">]</span> <span class="p">{</span> <span class="n">philosopher</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span> <span class="p">});</span> <span class="c1">// is captured by reference</span>
    <span class="n">std</span><span class="o">::</span><span class="n">jthread</span> <span class="nf">t2</span><span class="p">([</span><span class="o">&amp;</span><span class="p">]</span> <span class="p">{</span> <span class="n">philosopher</span><span class="p">(</span><span class="mi">2</span><span class="p">);</span> <span class="p">});</span>
    <span class="n">std</span><span class="o">::</span><span class="n">jthread</span> <span class="nf">t3</span><span class="p">([</span><span class="o">&amp;</span><span class="p">]</span> <span class="p">{</span> <span class="n">philosopher</span><span class="p">(</span><span class="mi">3</span><span class="p">);</span> <span class="p">});</span>
    <span class="n">std</span><span class="o">::</span><span class="n">jthread</span> <span class="nf">t4</span><span class="p">([</span><span class="o">&amp;</span><span class="p">]</span> <span class="p">{</span> <span class="n">philosopher</span><span class="p">(</span><span class="mi">4</span><span class="p">);</span> <span class="p">});</span>
<span class="p">}</span>
</code></pre></div></div> <p>Let’s break this solution down part by part. First thing to observe is that instead of assigning states to forks, Djikstra assigns 3 states to philosophers: <code class="language-plaintext highlighter-rouge">THINKING</code>, <code class="language-plaintext highlighter-rouge">HUNGRY</code>, and <code class="language-plaintext highlighter-rouge">EATING</code>. We have a mutex lock <code class="language-plaintext highlighter-rouge">critical_region_mtx</code> that protects checking philosopher states, transitioning states, and calling a special permission function <code class="language-plaintext highlighter-rouge">test</code>. There is also a binary semaphore for each philosopher in <code class="language-plaintext highlighter-rouge">both_forks_available</code> that represents both forks being available as a singular resource (<code class="language-plaintext highlighter-rouge">1</code> meaning both forks available, <code class="language-plaintext highlighter-rouge">0</code> meaning at least one fork is in use by a neighbor). The <code class="language-plaintext highlighter-rouge">both_forks_available</code> semaphores are initialized to <code class="language-plaintext highlighter-rouge">0</code> so no forks are available in the beginning (you will see why this is important soon).</p> <p>When a philosopher tries to eat, he must first enter the critical region to change to the <code class="language-plaintext highlighter-rouge">HUNGRY</code> state and check with <code class="language-plaintext highlighter-rouge">test</code> whether the left and right philosophers are eating. The <code class="language-plaintext highlighter-rouge">test</code> if successful (both neighbors not eating), releases the <code class="language-plaintext highlighter-rouge">both_forks_available</code> binary semaphore, essentially signaling that the two forks are now available to be picked up (permission given to self!). We see now that the reason the binary semaphores are initialized to <code class="language-plaintext highlighter-rouge">0</code> is because it requires the philosophers to first check the neighbor states before receiving permission. Once the <code class="language-plaintext highlighter-rouge">test</code> is conducted, the philosopher then can exit the critical region and attempt to acquire the forks by decrementing the binary semaphore. If <code class="language-plaintext highlighter-rouge">test</code> previously granted permission, this will succeed, otherwise the philosopher will be blocked.</p> <p>Now let’s look at what happens next with the two possibilities (permission granted or blocked):</p> <h3 id="permission-granted">Permission granted:</h3> <p>With the binary semaphore acquired, the philosopher has permission to pick up the forks without worrying about them being taken and can eat at his own pace (his <code class="language-plaintext highlighter-rouge">EATING</code> state bars neighbors from trying). When finished eating, the philosopher must put the fork down by entering the critical region to change to the <code class="language-plaintext highlighter-rouge">THINKING</code> state and then call <code class="language-plaintext highlighter-rouge">test</code> on behalf of the left and right neighbors. This is an important step because once the forks are freed, the left and right neighbors who may be blocked can then be released and given permission to eat.</p> <h3 id="blocked">Blocked:</h3> <p>If the philosopher is blocked, this means that one of the neighbors was in the <code class="language-plaintext highlighter-rouge">EATING</code> state when the <code class="language-plaintext highlighter-rouge">test</code> was conducted. However, it is guaranteed not to block forever because the neighbor once finished eating will call <code class="language-plaintext highlighter-rouge">test</code> on the blocked philosophers behalf. If both neighbors are not <code class="language-plaintext highlighter-rouge">EATING</code> at this point, the blocked philosopher is released and acquires the <code class="language-plaintext highlighter-rouge">both_forks_available</code> binary semaphore, gaining permission to eat!</p> <p>I do like this solution because while complicated, it is symmetric and guaranteed to be fair. If one of the neighbors is eating at the time of the <code class="language-plaintext highlighter-rouge">test</code> attempt, the philosopher can safely block on the binary semaphore and know that when the neighbor is finished, he will be notified and immediately given permission to eat.</p> <h1 id="mutex-locks-vs-binary-semaphores">Mutex Locks vs. Binary Semaphores</h1> <p>I think it is important to mention the distinction of mutexes and semaphores. When I initially learned about them, I thought locks and binary semaphores were the same. But there are fundamental differences in how they operate. With binary semaphores, anyone can call <code class="language-plaintext highlighter-rouge">signal()</code> on the semaphore and increment the semaphore to 1. Whereas with locks, the only person who can release it is the person who acquired the mutex. In short: a mutex has clear <strong>ownership property</strong>, while binary semaphores are <strong>agnostic to ownership</strong> and can be signalled by any thread.</p> <p>Binary semaphores are useful for signaling and coordination between threads (e.g. producer-consumer relationships) while mutexes are good for protecting critical sections.</p>]]></content><author><name></name></author><category term="tech"/><category term="code"/><category term="learning"/><category term="developer"/><summary type="html"><![CDATA[Understanding Djikstra's solution to the dining philosophers problem.]]></summary></entry><entry><title type="html">Symbolic Links</title><link href="https://davdma.github.io/blog/2025/Symlinks/" rel="alternate" type="text/html" title="Symbolic Links"/><published>2025-08-07T21:01:00+00:00</published><updated>2025-08-07T21:01:00+00:00</updated><id>https://davdma.github.io/blog/2025/Symlinks</id><content type="html" xml:base="https://davdma.github.io/blog/2025/Symlinks/"><![CDATA[<h1 id="inodes">Inodes</h1> <p>An important concept I learned in my Operating Systems CS class was the abstraction of <strong>inodes</strong> for the filesystem: a datastructure that contains <strong>metadata</strong> about a file or directory with pointers (direct, indirect, double indirect) to the underlying data blocks.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/inode-480.webp 480w,/assets/img/inode-800.webp 800w,/assets/img/inode-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/inode.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Simple diagram of an inode and how it points to the underlying file data. </div> <p>One nuance with inodes is the existence of <strong>symbolic links</strong> and <strong>hard links</strong>. I remember being really confused about this difference and asking my professor after class. I vaguely remember him explaining to me the details and that symbolic links are often the better choice over hard links. One message that was clear: hard links can be dangerous. However, I never really thought twice about it until recently, when I was trying to set up a git repo for my <code class="language-plaintext highlighter-rouge">vim</code> and <code class="language-plaintext highlighter-rouge">tmux</code> configuration files and saw a recommendation to make symlinks to the dot files (so <code class="language-plaintext highlighter-rouge">vim</code> or <code class="language-plaintext highlighter-rouge">tmux</code> can still find it when looking in root).</p> <h1 id="symbolic-vs-hard-links">Symbolic vs. Hard Links</h1> <p>So what is the difference? I had to do some googling to review it, but this visualization captures it perfectly:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/symlink-480.webp 480w,/assets/img/symlink-800.webp 800w,/assets/img/symlink-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/symlink.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Diagram that illustrates the subtle but key difference in how soft links and hard links work. </div> <p>They both function as shortcuts to files and directories, but how they do it is different. A soft or symbolic link is a file that points to another file <strong>via storing its file system path string as data</strong> (it has a different inode number). When the original file is deleted, the soft link is left dangling. If you create a new file with the same name, the soft link will become active again and attach to that file.</p> <p>Instead of storing the file path, a hard link <strong>points directly to the underlying inode</strong> of the original file (it has the same inode number). A good way to think about it is that every file starts with 1 hard link at creation time. The filesystem keeps track of how many hard links point to the inode (there is a <strong>hard link counter</strong> inside the inode metadata) so when you create another hard link, the counter is incremented. Only when all of the hard links are deleted will the data in the filesystem be freed. This can be dangerous if you lose track of all your hard links, and find that you cannot actually delete the file due to the counter being positive. It is also the case that sym links can cross filesystems while hard links cannot.</p> <h1 id="creating-symbolic-or-hard-links">Creating Symbolic or Hard Links</h1> <p>In Linux you can create sym links with the command:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">ln</span> <span class="nt">-s</span> &lt;target&gt; &lt;link_name&gt;
</code></pre></div></div> <p>This creates a sym link with <code class="language-plaintext highlighter-rouge">link_name</code> to the source or <code class="language-plaintext highlighter-rouge">target</code> file.</p> <p>For hard links just omit the <code class="language-plaintext highlighter-rouge">-s</code> flag:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">ln</span> &lt;target&gt; &lt;link_name&gt;
</code></pre></div></div> <p>So when I run the following command, I can have a symlink in root for <code class="language-plaintext highlighter-rouge">vim</code>, <code class="language-plaintext highlighter-rouge">bash</code> or <code class="language-plaintext highlighter-rouge">tmux</code> that links to the underlying file stored in my dotfiles repo.</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create a symlink</span>
<span class="nb">ln</span> <span class="nt">-s</span> ~/dotfiles/vimrc ~/.vimrc
</code></pre></div></div>]]></content><author><name></name></author><category term="tech"/><category term="code"/><category term="learning"/><category term="developer"/><summary type="html"><![CDATA[Understanding sym vs. hard links, how to use them]]></summary></entry><entry><title type="html">The Tmux Tool</title><link href="https://davdma.github.io/blog/2025/Learning-Tmux/" rel="alternate" type="text/html" title="The Tmux Tool"/><published>2025-08-02T18:11:00+00:00</published><updated>2025-08-02T18:11:00+00:00</updated><id>https://davdma.github.io/blog/2025/Learning-Tmux</id><content type="html" xml:base="https://davdma.github.io/blog/2025/Learning-Tmux/"><![CDATA[<h1 id="terminal-multiplexer">Terminal Multiplexer</h1> <p>The first time I encountered <code class="language-plaintext highlighter-rouge">tmux</code> was in my systems CS class. My professor was in the middle of an in class demonstration and mentioned that he liked to use this tool in the terminal called <code class="language-plaintext highlighter-rouge">tmux</code> because it made work a lot easier. He then proceeded to call <code class="language-plaintext highlighter-rouge">tmux</code> to manage his terminal session and run a bunch of code side by side. The whole thing was effortless and just looked like magic to me. I remember thinking to myself, “wow that IS really useful, maybe I should go learn how to use it myself”. Unfortunately I never quite found the time to - it was one of those things where once you put it to the side, you end up never coming back to it. I had forgotten about it until recently when I began to feel frustrated with my own workflow and the hassle of constantly creating new terminal windows and just going in and out of <code class="language-plaintext highlighter-rouge">vim</code> and other tools in the command line.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/tmuxcover-480.webp 480w,/assets/img/tmuxcover-800.webp 800w,/assets/img/tmuxcover-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/tmuxcover.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <h1 id="commands">Commands</h1> <p>To start a <code class="language-plaintext highlighter-rouge">tmux</code> server with the default session (session 0), just run:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tmux
</code></pre></div></div> <p>To detach from your current <code class="language-plaintext highlighter-rouge">tmux</code> session the key stroke is <strong>Ctrl+B</strong> then <strong>D</strong> (it’s a little awkward so people do configure this). The <strong>Ctrl+B</strong> key stroke patterns only work inside active <code class="language-plaintext highlighter-rouge">tmux</code> sessions. To see all <code class="language-plaintext highlighter-rouge">tmux</code> sessions use:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tmux <span class="nb">ls</span>
</code></pre></div></div> <p>The cool thing about <code class="language-plaintext highlighter-rouge">tmux</code> is that when sshing onto a server, the tmux process can be started on the server. With the background process running, even if you disconnect from ssh and then come back and reattach, the process will still be running! You can simply reconnect to the server and reattach to the session with:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tmux attach <span class="nt">-t</span> 0
</code></pre></div></div> <p>You can abbreviate:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tmux a <span class="nt">-t</span> 0
</code></pre></div></div> <p>To create a new session and name it:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tmux new <span class="nt">-s</span> mysession
</code></pre></div></div> <p>To kill a session:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tmux kill-sess <span class="nt">-t</span> mysession
</code></pre></div></div> <h2 id="sessions-windows-and-panes">Sessions, Windows, and Panes</h2> <p>It’s useful to have the model of <code class="language-plaintext highlighter-rouge">session -&gt; window -&gt; pane</code>.</p> <p>You can have multiple windows in each session (think of windows as tabs in a browser). You can add new windows with <strong>Ctrl+B</strong> followed by <strong>C</strong>. Jump to the specified window number with <strong>Ctrl+B</strong> followed by <strong>2</strong> for window 2 or use <strong>Ctrl+B</strong> followed by <strong>N</strong> or <strong>P</strong> to go forward or back. To kill the window use <strong>Ctrl+B</strong> then <strong>&amp;</strong>.</p> <p>Within each of these windows you can also split the session window into horizontal or vertical panes using <strong>Ctrl+B</strong> followed by <strong>%</strong> for horizontal and <strong>“</strong> for vertical. <strong>Ctrl+B</strong> with arrow keys then moves between the panes. Users often recommend enabling mouse support for <code class="language-plaintext highlighter-rouge">tmux</code> by modifying the config file so you can resize panes more easily.</p> <p>One of the shortcuts to be careful of is <strong>Ctrl+D</strong> which exits (terminates) that <code class="language-plaintext highlighter-rouge">tmux</code> session you are in. It’s easy to accidentally hit that key stroke while trying to detach.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/tmux-480.webp 480w,/assets/img/tmux-800.webp 800w,/assets/img/tmux-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/tmux.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Cheatsheet of all tmux commands. </div> <p>Another feature to be aware of is the ability to select text in your windows and then copy and paste them. The <code class="language-plaintext highlighter-rouge">extracto</code> plugin is nice because it allows you to fuzzy select and not have to manually select it.</p> <h1 id="plugins--themes">Plugins &amp; Themes</h1> <p>There are also some cool themes and useful plugins out there for configuring the <code class="language-plaintext highlighter-rouge">tmux</code> experience:</p> <ul> <li><a href="https://github.com/laktak/extrakto">extracto</a></li> <li><a href="https://github.com/nordtheme/tmux">arctic theme</a></li> </ul> <p>I would also recommend using the <code class="language-plaintext highlighter-rouge">tmux</code> plugin manager <a href="https://github.com/tmux-plugins/tpm">here</a> instead of manually downloading them.</p> <p>My <code class="language-plaintext highlighter-rouge">.tmux.conf</code> file looks like this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># improve colors
set -g default-terminal 'screen-256color'

# prefix as Ctrl+space easier than Ctrl+b
unbind C-b
set-option -g prefix C-Space
bind-key C-Space send-prefix

# start window numbers at 1 to match keyboard order with tmux window order
set -g base-index 1
set-window-option -g pane-base-index 1

# renumber windows sequentially after closing any of them
set -g renumber-windows on

# split panes using | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# switch panes using Alt-arrow without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# increase scrollback lines
set -g history-limit 10000

# enable mouse
set -g mouse on

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'laktak/extrakto'
set -g @plugin 'wfxr/tmux-power'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
</code></pre></div></div> <p>To reload the current <code class="language-plaintext highlighter-rouge">tmux</code> config, run:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tmux source-file ~/.tmux.conf
</code></pre></div></div> <p>Note: setting up extrakto requires <code class="language-plaintext highlighter-rouge">fzf</code> (this involves downloading with brew, adding a line to <code class="language-plaintext highlighter-rouge">.bashrc</code>).</p> <h1 id="resources">Resources</h1> <p>Some tutorials I consulted:</p> <ul> <li><a href="https://www.redhat.com/en/blog/introduction-tmux-linux">Red Hat guide</a></li> <li><a href="https://dev.to/iggredible/tmux-tutorial-for-beginners-5c52">Dev tutorial</a></li> <li><a href="https://hamvocke.com/blog/a-guide-to-customizing-your-tmux-conf/">Customizing Tmux</a></li> </ul>]]></content><author><name></name></author><category term="tech"/><category term="code"/><category term="developer"/><summary type="html"><![CDATA[Teaching myself to use the terminal multiplexer]]></summary></entry><entry><title type="html">Learning Git</title><link href="https://davdma.github.io/blog/2025/Learning-Git/" rel="alternate" type="text/html" title="Learning Git"/><published>2025-07-30T00:00:00+00:00</published><updated>2025-07-30T00:00:00+00:00</updated><id>https://davdma.github.io/blog/2025/Learning-Git</id><content type="html" xml:base="https://davdma.github.io/blog/2025/Learning-Git/"><![CDATA[<h2 id="how-i-started">How I Started</h2> <p>I remember before starting college, <code class="language-plaintext highlighter-rouge">Git</code> was a scary word to me. Mainly because I didn’t understand what it was and why it was important. All the <code class="language-plaintext highlighter-rouge">Git</code> commands seemed strange and obscure. I would be even more confused when people brought up Github. What’s the difference between <code class="language-plaintext highlighter-rouge">Git</code> and Github? Aren’t they the same thing? Why is one in the terminal and the other a website? I had lots of unanswered questions.</p> <p>During a meeting with a University of Chicago grad who was working on Github developer docs, I told him how daunting <code class="language-plaintext highlighter-rouge">Git</code> felt to me. I asked him how he was able to learn <code class="language-plaintext highlighter-rouge">Git</code>, and whether there was a class out there or a specific resource to become familiar with it. He responded that I shouldn’t worry too much, and that eventually I would pick it up as a student in college. I was a little skeptical.</p> <p>But he turned out to be right.</p> <hr/> <h2 id="the-basics">The Basics</h2> <p>One of the first things I learned through my intro CS class was how to use <code class="language-plaintext highlighter-rouge">Git</code> to clone, commit, and push my local work to a remote repository hosted on Github. This helped me understand the difference between <code class="language-plaintext highlighter-rouge">Git</code> and Github. <code class="language-plaintext highlighter-rouge">Git</code> is the essential version control system that lives on your computer, and Github is simply a service built around <code class="language-plaintext highlighter-rouge">Git</code> that hosts your code and helps you share your <code class="language-plaintext highlighter-rouge">Git</code> managed repositories. We could do without Github, but we cannot do without <code class="language-plaintext highlighter-rouge">Git</code> - that’s the tool you should know how to use.</p> <p>To get a local git repository started, you can clone (copy) an existing remote repository:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/davdma/davdma.github.io.git
</code></pre></div></div> <p>You can see the modified states of your files using <code class="language-plaintext highlighter-rouge">git status</code>. Then you could commit changes by running:</p> <ol> <li> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add file.py
</code></pre></div> </div> </li> <li> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git commit <span class="nt">-m</span> <span class="s2">"my first commit"</span>
</code></pre></div> </div> </li> </ol> <p>To upload to the remote repository you just run:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push
</code></pre></div></div> <p>To pull in changes from the repo (when professors would add files we needed), run:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git pull
</code></pre></div></div> <p>These commands were all that was needed for class.</p> <p>Note: If you wanted to start a new <code class="language-plaintext highlighter-rouge">git</code> repository locally on your computer instead of cloning an existing one, you can just call <code class="language-plaintext highlighter-rouge">git init</code> in the project directory. You can always add the remote later using the <code class="language-plaintext highlighter-rouge">git remote</code> command.</p> <hr/> <h2 id="things-to-know">Things to Know</h2> <p>Some things that helped me better wrap my head around what was going on in <code class="language-plaintext highlighter-rouge">Git</code>:</p> <ul> <li>Files are either tracked or untracked. Tracked means that <code class="language-plaintext highlighter-rouge">Git</code> knows about it, so you will want to make sure your most important pieces of code are being tracked. Untracked files are just everything else in the directory (if you lose untracked files, <code class="language-plaintext highlighter-rouge">Git</code> cannot recover them for you). You can check the status of your files using <code class="language-plaintext highlighter-rouge">git status</code>.</li> <li>Terms you will hear a lot is <strong>index</strong> and <strong>working tree</strong>: <ul> <li><strong>Staging area (Index)</strong> is the temporary area where you prepare changes to be included in the next commit. It is the bridge between the working tree and the repository’s history. Each time you run <code class="language-plaintext highlighter-rouge">git add</code> it sends a snapshot of your working tree to put in the index. The purpose of the index is that it allows you to selectively stage changes for including in the next commit without doing it all at once.</li> <li>The <strong>working directory (Working Tree)</strong> is the directory on your file system where the project files live. The working tree contains your tracked and untracked files and any modifications you make. The changes you make in your working directory or tree are not tracked until explicitly added to the index.</li> </ul> </li> <li>The mental model I use is <code class="language-plaintext highlighter-rouge">working tree -&gt; index -&gt; git history</code>. The first two is bridged with <code class="language-plaintext highlighter-rouge">git add</code>, and the second two is bridged with <code class="language-plaintext highlighter-rouge">git commit</code>.</li> <li>Remotes are versions of your repository hosted online. In many cases, you will need to handle remotes, such as adding or removing remotes. To see what remotes you have, run <code class="language-plaintext highlighter-rouge">git remote -v</code> (the flag displays the remote url next to the remote name). You can push and pull from any of these remotes! <ul> <li>To add remotes use <code class="language-plaintext highlighter-rouge">git remote add &lt;name&gt; &lt;url&gt;</code>. You can give it any name you want as long as it’s short and easy to remember.</li> <li>You can later specify the remote when pushing with <code class="language-plaintext highlighter-rouge">git push &lt;remote&gt; &lt;branch&gt;</code> e.g. <code class="language-plaintext highlighter-rouge">git push origin master</code>.</li> </ul> </li> <li>When you are working on a remote repository, it is important to know that there is a <strong>local representation</strong> of the remote repository that lives on your computer (if your local branch is <code class="language-plaintext highlighter-rouge">main</code> then it is typically referenced by <code class="language-plaintext highlighter-rouge">origin/main</code>). Each time you <code class="language-plaintext highlighter-rouge">git pull</code> it actually runs two separate commands, <code class="language-plaintext highlighter-rouge">git fetch</code> and <code class="language-plaintext highlighter-rouge">git merge</code> behind the scenes. <code class="language-plaintext highlighter-rouge">git fetch</code> (kind of like a download) synchronizes your local remote representation with the online hosted remote representation. To get the updates in your local files the <code class="language-plaintext highlighter-rouge">git merge</code> is then done between the new local remote branch and your local branch.</li> </ul> <hr/> <h2 id="undoing-things">Undoing Things</h2> <p>This is one of the things that stumped me for a while. How do I undo a <code class="language-plaintext highlighter-rouge">git add</code>? What about a particular commit? How do I revert a specific file back to a previous state rather than my entire repo?</p> <p>If you want to unstage a file you have staged with <code class="language-plaintext highlighter-rouge">git add</code>, run:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git reset HEAD &lt;file&gt;
</code></pre></div></div> <p>OR</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git restore <span class="nt">--staged</span> &lt;file&gt;
</code></pre></div></div> <p>If you want to discard changes to a tracked file, you can revert to a previous commit of the file with:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout <span class="nt">--</span> &lt;file&gt;
</code></pre></div></div> <p>OR</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git restore &lt;file&gt;
</code></pre></div></div> <p>(Note: HEAD is not moved when you just revert a single file like this.)</p> <p>For resetting the entire repo state (i.e. including your index and tracked files) to a previous commit use:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git reset &lt;mode&gt; &lt;commit&gt;<span class="sb">`</span><span class="nb">.</span>
</code></pre></div></div> <p>Definitely be careful with using commands that discard unwanted changes, as they may not be recovered. For instance, when using <code class="language-plaintext highlighter-rouge">git reset &lt;commit&gt;</code> you need to be aware that there are three distinct settings, <code class="language-plaintext highlighter-rouge">--soft</code>, <code class="language-plaintext highlighter-rouge">--mixed</code> (default), and <code class="language-plaintext highlighter-rouge">--hard</code>. The soft setting does <strong>not change your index or working tree</strong>, but simply moves the HEAD to a previous commit, so you will still have everything you staged ready to be committed. The mixed setting <strong>resets the index</strong> but not the working tree, so you still have all your changes, just not added for commit. The hard setting resets <strong>both the index and working tree</strong>, so that it permanently discards all of the changes you’ve made in your filesystem and reverts everything to that specific commit – this is the most dangerous, so use with caution.</p> <p>You might wonder why there is both a <code class="language-plaintext highlighter-rouge">git reset</code> and <code class="language-plaintext highlighter-rouge">git restore</code>. <code class="language-plaintext highlighter-rouge">git restore</code> is an alternative and is preferable for newer versions of <code class="language-plaintext highlighter-rouge">git</code>. But there are some intricacies to them which I learned while trying to revert just a single file to a previous state. They sound similar, but are actually doing different things. I learned that <code class="language-plaintext highlighter-rouge">git reset</code> moves the <code class="language-plaintext highlighter-rouge">HEAD</code> while <code class="language-plaintext highlighter-rouge">git restore</code> does not, it only modifies your working directory. This is why <code class="language-plaintext highlighter-rouge">git restore</code> is a safer operation.</p> <p>A useful command when it comes to probing around at file states is <code class="language-plaintext highlighter-rouge">git diff</code>. It shows you the difference between files at particular states. By itself without any flags it helps you look at changes between your <strong>working tree</strong> and the <strong>index</strong> staging area. If you wanted to look at changes between your <strong>index</strong> and a prior commit use <code class="language-plaintext highlighter-rouge">git diff --staged &lt;commit&gt;</code> (or <code class="language-plaintext highlighter-rouge">--cached</code> which is a synonym of <code class="language-plaintext highlighter-rouge">--staged</code>). For changes between <strong>working tree</strong> and a particular commit, use <code class="language-plaintext highlighter-rouge">git diff --merge-base &lt;commit&gt;</code>. Again, here you see why it is good to know the difference between the working tree and the index.</p> <p>If you just want to see the changes introduced at a commit before you undo it, run:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git show &lt;commit-sha&gt;
</code></pre></div></div> <hr/> <h2 id="branching">Branching</h2> <p>Branching is the most powerful feature in <code class="language-plaintext highlighter-rouge">Git</code>, and I wish I learned about why sooner. During my software development class, I was working alongside multiple teams of students developing new features for the codebase. When you have many individuals all working on different versions of the code on your <code class="language-plaintext highlighter-rouge">main</code> branch, things can get hairy quick. This is why it is essential to work with different branches. Branching allows you to work on the codebase separately without affecting the main code if something breaks. In this workflow, typically the <code class="language-plaintext highlighter-rouge">main</code> branch (which you might be accustomed to working with) is protected, and developers cannot directly make commits to <code class="language-plaintext highlighter-rouge">main</code>. What you must do is branch off of <code class="language-plaintext highlighter-rouge">main</code>, work on that branch and make commits to it separately, and then incorporate your code later when it is ready through a <strong>pull request (PR)</strong>. The PR must be reviewed by other developers before it is finally merged into the <code class="language-plaintext highlighter-rouge">main</code> branch.</p> <p>The process of branching and merging is essential to the concept of <strong>Continuous Integration (CI)</strong> which is part of the software development process. CI prevents integration hell by frequently merging each developer’s work into the mainline branch.</p> <p>To create a new branch from your current commit, run:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git branch &lt;name&gt;
</code></pre></div></div> <p>OR</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout <span class="nt">-b</span> &lt;name&gt;
</code></pre></div></div> <p>You can switch branches using the command:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout &lt;name&gt;
</code></pre></div></div> <p>If you just created a branch, you will need to set the upstream remote in order to do a <code class="language-plaintext highlighter-rouge">git push</code>. To set it, run:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push <span class="nt">-u</span> &lt;remote&gt; &lt;branch&gt;
</code></pre></div></div> <p>Note: <code class="language-plaintext highlighter-rouge">-u</code> is short for <code class="language-plaintext highlighter-rouge">--set-upstream</code>.</p> <p>Merging branches takes all the changes from one branch and adds it to another using a merge commit. If you are on the <code class="language-plaintext highlighter-rouge">david</code> branch and you call <code class="language-plaintext highlighter-rouge">git merge main</code>, you merge the changes from the <code class="language-plaintext highlighter-rouge">main</code> branch over to your <code class="language-plaintext highlighter-rouge">david</code> branch. If on the other hand, you wanted to merge <code class="language-plaintext highlighter-rouge">david</code> onto <code class="language-plaintext highlighter-rouge">main</code> you would need to checkout <code class="language-plaintext highlighter-rouge">main</code> and <code class="language-plaintext highlighter-rouge">git merge david</code> from there. Often times you will want to add the newest changes from <code class="language-plaintext highlighter-rouge">main</code> to the side branch you are working on - to do this, you have to checkout <code class="language-plaintext highlighter-rouge">main</code> and call <code class="language-plaintext highlighter-rouge">git pull</code> to get the recent updates to <code class="language-plaintext highlighter-rouge">main</code>, then checkout the <code class="language-plaintext highlighter-rouge">david</code> side branch again to merge the updates from <code class="language-plaintext highlighter-rouge">main</code> in.</p> <p>Often times you will encounter merge conflicts. This can be daunting at first, but it is actually straightforward once you learn how to resolve merge conflicts in the editor. At locations of merge conflicts there will be conflict markers <code class="language-plaintext highlighter-rouge">&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code>, <code class="language-plaintext highlighter-rouge">=======</code>, <code class="language-plaintext highlighter-rouge">&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code>. While most editors give the option to choose between the current or incoming change, <strong>you are free to modify the lines as you wish</strong> (e.g. if you want to choose the incoming change but make modifications or create a combination of the current and incoming change). A common misconception is that you have to choose one or the other, when you can rewrite it however you like. Once resolved and all conflict markers are removed, save the file and stage it, then continue with <code class="language-plaintext highlighter-rouge">git merge --continue</code>.</p> <hr/> <h2 id="the-powerful-git-rebase">The Powerful Git Rebase</h2> <p>During a talk by a software developer at Slack, he recommended that students learn to use the interactive git rebase <code class="language-plaintext highlighter-rouge">git rebase -i</code>. Apparently nobody knows how to use it, but it’s a superpower. This piqued my interest, so I started learning more about rebasing. So… what is rebasing?</p> <p>A rebase is another way to combine work between branches in addition to the merge. While a merge joins two branches together in an entangled fashion, a rebase creates a linearized commit history, by taking a set of commits and copying them over on top of the branch you want. In real life codebases, merging work from many developers can get gross really fast. Rebasing makes things much cleaner and easier.</p> <p>Usually you would run <code class="language-plaintext highlighter-rouge">git rebase &lt;upstream&gt; &lt;branch&gt;</code>. But if you run <code class="language-plaintext highlighter-rouge">git rebase &lt;arg&gt;</code> it will automatically assume that the argument name is the upstream branch and you want the current branch you have checked out to be rebased onto it. For example calling <code class="language-plaintext highlighter-rouge">git rebase main</code> from <code class="language-plaintext highlighter-rouge">david</code> would move the work from the <code class="language-plaintext highlighter-rouge">david</code> branch on top of the <code class="language-plaintext highlighter-rouge">main</code> branch.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/gitblog-480.webp 480w,/assets/img/gitblog-800.webp 800w,/assets/img/gitblog-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/gitblog.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> Notice that once bugFix has already been rebased onto main, rebasing main onto bugFix simply forwards the HEAD. At step 3, the main branch has all the changes from bugFix integrated with cleaner commit history than a merge. </div> <p>If you want to move work around by copying a series of commits below your current location or HEAD and you know the exact commits you want, instead of rebase you can run:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git cherry-pick &lt;commit1&gt; &lt;commit2&gt; &lt;commit3&gt;
</code></pre></div></div> <p>But if you are not sure what commits you want or their hashes, interactive rebase comes in (and is more powerful). Interactive rebase can let you reorder commits, drop or keep commits, squash commits and even edit commits. When you run <code class="language-plaintext highlighter-rouge">git rebase -i</code>, you will first be dropped into your default editor (in my case vim) with the following lines:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pick 1a2b3c Commit message A
pick 4d5e6f Commit message B
pick 7g8h9i Commit message C
</code></pre></div></div> <p>You can then choose what you want to do with each commit by modifying the prefix before the hash. For instance, the prefix <code class="language-plaintext highlighter-rouge">pick</code> means you want to keep the commit. You can also use <code class="language-plaintext highlighter-rouge">drop</code> to omit the commit, <code class="language-plaintext highlighter-rouge">edit</code> to pause and make changes, or <code class="language-plaintext highlighter-rouge">reword</code> to keep commit but modify its commit message. Once you’ve made the appropriate changes for the rebase instructions, save and exit, and rebase will run. Most likely you will encounter merge conflicts in the process, in which case the rebase will pause at that point. You will have to resolve these conflicts manually, add those resolutions to your index with <code class="language-plaintext highlighter-rouge">git add</code>, and then continue on by running <code class="language-plaintext highlighter-rouge">git rebase --continue</code>. If things get messy, you can always reset and try again with <code class="language-plaintext highlighter-rouge">git rebase --abort</code>.</p> <p>If you have completed the rebase but want to go back, you can always undo the rebase by looking into the ref log with <code class="language-plaintext highlighter-rouge">git reflog</code> and doing a <code class="language-plaintext highlighter-rouge">git reset</code>.</p> <p>–</p> <h2 id="useful-commands">Useful Commands</h2> <p>If you are jumping around commits and have uncommitted changes in your files you might lose, you will want to use <code class="language-plaintext highlighter-rouge">git stash</code>. Suppose you want to go back 3 commits to <code class="language-plaintext highlighter-rouge">HEAD~3</code>. If you don’t want to commit your current changes you will have to stash them first:</p> <ol> <li> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git stash
</code></pre></div> </div> </li> <li> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout HEAD~3 <span class="c"># now you can switch</span>
</code></pre></div> </div> </li> <li> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git checkout feature <span class="c"># come back to your work</span>
</code></pre></div> </div> </li> <li> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git stash pop <span class="c"># restores the changes you stashed</span>
</code></pre></div> </div> </li> </ol> <p>Once you call <code class="language-plaintext highlighter-rouge">git stash</code> those changes will disappear and be stashed away. When <code class="language-plaintext highlighter-rouge">git stash pop</code> is called they will be put back in your working directory.</p> <p>You can inspect your reference history with <code class="language-plaintext highlighter-rouge">git reflog</code>. The reference logs or <code class="language-plaintext highlighter-rouge">reflog</code> record useful information about where the <code class="language-plaintext highlighter-rouge">HEAD</code> was several moves ago, as well as the movement of branch references in the local repository. It also stores recent actions. The <code class="language-plaintext highlighter-rouge">reflog</code> syntax <code class="language-plaintext highlighter-rouge">@{}</code> is important to know in addition to the <code class="language-plaintext highlighter-rouge">^</code> and <code class="language-plaintext highlighter-rouge">~</code> symbols: <code class="language-plaintext highlighter-rouge">HEAD@{n}</code> refers to the position of HEAD in the reflog <code class="language-plaintext highlighter-rouge">n</code> moves ago. Check with the reflog command what <code class="language-plaintext highlighter-rouge">HEAD@{n}</code> points to.</p> <p>Move the HEAD to a previous reference point with all changes staged:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git reset <span class="nt">--soft</span> <span class="s2">"HEAD@{2}"</span>
</code></pre></div></div> <hr/> <h2 id="flags-i-like">Flags I Like</h2> <ul> <li>Typically for large repos with lots of files that I don’t want to store on Github, I end up with many untracked files. This can clutter the <code class="language-plaintext highlighter-rouge">git status</code> output. Using the flag <code class="language-plaintext highlighter-rouge">git status -uno</code> however skips displaying untracked files, making it easier to read.</li> <li>Sometimes you want to just add and commit everything you modified. Save yourself some time with <code class="language-plaintext highlighter-rouge">git commit -a</code> flag. You don’t even need to <code class="language-plaintext highlighter-rouge">git add</code>!</li> <li>For cleaner commit history when incorporating remote changes, fetch and rebase instead of the default fetch and merge with <code class="language-plaintext highlighter-rouge">git pull --rebase</code> option.</li> <li>For reading the commit logs, show diffs with <code class="language-plaintext highlighter-rouge">git log -p</code>. Abbreviated stats (lines modified etc.) can be shown with <code class="language-plaintext highlighter-rouge">--stat</code> and the ASCII graph with <code class="language-plaintext highlighter-rouge">--graph</code>. Can also limit to specific files with <code class="language-plaintext highlighter-rouge">git log -- path/to/file</code>.</li> <li>For reading diffs with more context, use the <code class="language-plaintext highlighter-rouge">-U</code> flag, add the number of lines you want around the diffs with an integer after the flag, for example, <code class="language-plaintext highlighter-rouge">git diff -U8</code> for 8 lines of context instead of the default 3.</li> <li>For seeing what git files you currently have tracked you can use <code class="language-plaintext highlighter-rouge">git ls-files .</code>.</li> </ul> <hr/> <h2 id="resources">Resources</h2> <p>Here are some useful resources I consulted (and I still often go back to) on my journey to learning git:</p> <ul> <li><a href="https://uchicago-cs.github.io/student-resource-guide/tutorials/git-local.html">The UChicago Student Resource Guide</a></li> <li><a href="https://learngitbranching.js.org/">Interactive Git Tutorial</a></li> <li><a href="https://git-scm.com/book/en/v2">Pro Git book</a></li> <li><a href="https://www.youtube.com/watch?v=f1wnYdLEpgI&amp;ab_channel=TheModernCoder">Learn Git Rebase</a></li> </ul>]]></content><author><name></name></author><category term="code"/><category term="learning"/><summary type="html"><![CDATA[Getting over my fear of git]]></summary></entry><entry><title type="html">Quote Wall</title><link href="https://davdma.github.io/blog/2025/Quote-Wall/" rel="alternate" type="text/html" title="Quote Wall"/><published>2025-07-29T22:40:00+00:00</published><updated>2025-07-29T22:40:00+00:00</updated><id>https://davdma.github.io/blog/2025/Quote-Wall</id><content type="html" xml:base="https://davdma.github.io/blog/2025/Quote-Wall/"><![CDATA[<h1 id="things-i-heard">Things I Heard</h1> <p>While listening to a podcast episode interviewing Bret Taylor (one of the leading engineers behind the development of Google Maps):</p> <blockquote> <p>If someone says something I’m not familiar with, I’ve gotten over the feeling dumb thing, I’m like - I don’t know what that is, explain that to me. —Bret Taylor</p> </blockquote> <p>I have a good friend who epitomizes the type A personality. No matter how busy he is and how much responsibility he takes on, he always finds time to host events, practice the piano, read books, cook new recipes, and more. I often find myself in awe at this. When I asked how he’s able to do so much, he replied (and I paraphrase):</p> <blockquote> <p>The way I get so much done with my time is that I set out a chunk of time to do something and I just sit down and do it. I don’t let myself get distracted. It takes discipline. —Friend</p> </blockquote> <p>I think one of the most notable pieces of advice I’ve gotten from talks by software developers while as a student at the University of Chicago is the emphasis on documentation and readability:</p> <blockquote> <p>Your code in industry is read 10,000 times more than it is edited - readibility is more important than being smart. —Mark Cohen</p> </blockquote> <p>My scivis and datavis CS professor Gordon Kindlmann (one of my favorite professors) would give a motivating spiel at the start of every class on what it means to be a good computer scientist, engineer, and learner. It almost always involved quotes from famous software engineers past and present e.g. Margaret Hamilton or Leslie Lamport. But funnily enough, the most memorable one was when he pulled up a TikTok of someone named Dr. Becky as part of his class warmup. He told the class that in this TikTok, Dr. Becky succinctly explained something important and fundamental to what it means to grow and overcome challenges on the path to becoming more capable.</p> <blockquote> <p>The Learning Space is the space between not knowing, and knowing. But in between, it’s very hard. In that space it’s instinctual to want to avoid that frustrating and uncomfortable feeling. But the next time you feel that way, recognize that it is a sign that you are learning something new. Only by embracing this feeling and pushing forward can you become the person that knows the answer. —Gordon Kindlmann</p> </blockquote> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/beckyquote-480.webp 480w,/assets/img/beckyquote-800.webp 800w,/assets/img/beckyquote-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/beckyquote.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <div class="caption"> A screenshot of the slide in question from Professor Kindlmann's lecture (DataVis25). </div> <p>I went to a talk titled “God, the Grind, and the Good Life” by my social psychology professor, Professor Oishi (another one of my favorite professors). An important principle that he applies in his own life when work gets difficult is that he replaces “have to” with “get to” in his mind. Instead of “I have to finish this paper” he thinks “I get to finish this paper” because not everyone gets that opportunity to work on interesting research projects and publish their results. He also emphasizes stepping away from the hedonic treadmill by maximizing success while managing expectation and ambition. As success grows, often ambition grows as well, and if we use the analogy of self esteem as success divided by ambition, if you don’t curb your ambitions, you will never be satisfied. At the end of the day, subjectivity matters the most when it comes to reconciling work with the good life.</p> <blockquote> <p>If you believe you are living a good life, that’s all that matters. —Shigehiro Oishi</p> </blockquote> <blockquote> <p>Happiness is not intensity but frequency. Small and frequent happy moments in enduring relationships make a good life, not promotions and big life events. —Shigehiro Oishi</p> </blockquote>]]></content><author><name></name></author><category term="philosophy"/><category term="reflection"/><summary type="html"><![CDATA[Some things I heard that resonated]]></summary></entry><entry><title type="html">Embracing AI Tools</title><link href="https://davdma.github.io/blog/2025/AI-Tools/" rel="alternate" type="text/html" title="Embracing AI Tools"/><published>2025-07-29T18:11:00+00:00</published><updated>2025-07-29T18:11:00+00:00</updated><id>https://davdma.github.io/blog/2025/AI-Tools</id><content type="html" xml:base="https://davdma.github.io/blog/2025/AI-Tools/"><![CDATA[<h1 id="ai-coding-assistants">AI Coding Assistants</h1> <p>I think we have entered the golden age of AI developer tools. Just in AI coding assistants alone I’m quickly losing track of the options that are out there, from Wind Surf to Cline to Claude Code to Cursor. The list goes on. Recently, Amazon released a new AI IDE called <a href="https://kiro.dev/blog/introducing-kiro/">Kiro</a>, which I learned is another VSCode fork. This made me quite curious as to how more money and capital is being funneled into a seemingly concentrated space already full of viable options. But each AI coding assistant is quite unique in what they offer. Kiro’s selling point, for instance, is that it offers a feature called “Spec” which gives you the chance to build out your prompt through listing requirements, design, and task to ensure higher quality code generation. Other AI IDEs simply take the prompt and run with it no questions asked - for better or worse. This kind of rigorous approach focusing on documentation and maintainability is more suited to the software development life cycle for building production-ready code and makes the AI coding experience much more frictionless (less likely that the LLM misunderstands your prompt’s intent), at the cost of some added tool complexity.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/aitoolsblog-480.webp 480w,/assets/img/aitoolsblog-800.webp 800w,/assets/img/aitoolsblog-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/aitoolsblog.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>I think with all of these AI tools, people should really try their hand at a couple and see what they like. I was quite stubbornly using Cursor for a while (I loved the Tab), so it took me a long time to give a different AI coding assistant a shot. During my research internship at Argonne, my mentor Jeremy Feinstein was a big proponent of a CLI tool called <a href="https://aider.chat/">Aider</a>. It’s an open source AI coding assistant that lives completely in your terminal. It is also very easy to configure by setting up your API keys to your favorite LLMs whether that is Deepseek or Gemini or Claude. I realized that AI coding without an IDE could also be just as smooth and effective. Aider is also easy to pickup, as it just involves using a couple in chat commands. I was impressed when I used Aider to quickly run an experiment involving Conditional Variational Autoencoders (I was curious as to the role of the encoder conditioning in training on decoder generation), and it was all done in one shot.</p>]]></content><author><name></name></author><category term="tech"/><category term="code"/><category term="developer"/><summary type="html"><![CDATA[AI coding assistants, interesting podcast tidbits]]></summary></entry></feed>